# Система управління ремонтною майстернею електроніки

Курсовий проект для керування ремонтами електроніки. Система дозволяє реєструвати замовлення, контролювати етапи ремонту, вести облік деталей, завершувати ремонт із розрахунком вартості та надсилати клієнтам Email/SMS-сповіщення.

## Основні можливості

- Авторизація працівників через JWT.
- Розмежування прав доступу для адміністратора, менеджера та майстра.
- Реєстрація замовлення на ремонт.
- Контроль статусів: `прийнято`, `діагностика`, `ремонт`, `виконано`, `видано`.
- Ручне призначення відповідального майстра для адміна та менеджера.
- Журнал історії статусів із датою, попереднім/новим статусом і користувачем.
- Облік клієнтів, пристроїв, майстрів і використаних деталей.
- Автоматичне списання деталей зі складу при завершенні ремонту.
- Заявки майстрів на замовлення деталей зі сповіщенням для адміністратора.
- Розрахунок суми до оплати: робота майстра + використані деталі.
- Звіти з фільтром за датами, доходом, завантаженням майстрів і використаними деталями.
- Email-сповіщення з оновленим дизайном і сумою до оплати.
- SMS-сповіщення через Twilio при прийнятті та виконанні замовлення.
- Автоматичне створення структури бази даних при старті сервера.

## Структура проекту

```text
electronics-repair-system/
  client/                    # React + Vite клієнтська частина
    src/
      components/            # Сторінки та UI-компоненти
      styles/                # CSS-стилі
      assets/                # Локальні ресурси клієнта
  server/                    # Node.js + Express сервер
    src/
      app.js                 # Express-застосунок і підключення маршрутів
      config/                # Конфігурація
      database/              # Pool, схема БД, bootstrap
      middleware/            # JWT middleware
      routes/                # API-маршрути
      services/              # Email, SMS, майстри
      utils/                 # Валідація, статуси
    index.js                 # Точка запуску сервера
    .env.example             # Приклад змінних середовища
```

## Вимоги

- Node.js
- PostgreSQL
- npm
- Обліковий запис Gmail з app password для Email-сповіщень
- Обліковий запис Twilio для SMS-сповіщень

## Налаштування сервера

Перейдіть у папку сервера:

```bash
cd server
npm install
```

Створіть файл `.env` на основі `.env.example`:

```env
DB_USER=postgres
DB_PASSWORD=your-postgres-password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=repair_workshop
DB_ADMIN_DATABASE=postgres
DB_AUTO_CREATE=true
PORT=5000
JWT_SECRET=replace-with-long-random-secret
DEFAULT_USER_PASSWORD=123456

EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password

TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=...
TWILIO_PHONE_NUMBER=+1...
```

Важливо: `.env` не потрібно комітити в Git, бо там зберігаються паролі та токени.

## База даних

PostgreSQL має бути встановлений і запущений. При старті сервер:

- підключається до PostgreSQL;
- якщо `DB_AUTO_CREATE=true`, пробує створити базу `DB_NAME`;
- створює таблиці, якщо їх ще немає;
- додає стартових майстрів, запчастини та користувачів;
- не видаляє існуючі замовлення або клієнтів.

Таблиці бази даних:

- `clients` - клієнти;
- `devices` - пристрої клієнтів;
- `orders` - ремонтні замовлення;
- `spare_parts` - склад деталей;
- `order_parts` - використані деталі в замовленні;
- `order_status_history` - історія зміни статусів замовлень;
- `users` - користувачі системи;
- `masters` - майстри.

## Запуск

Сервер:

```bash
cd server
npm run dev
```

Клієнт:

```bash
cd client
npm install
npm run dev
```

Після запуску:

- API: `http://localhost:5000/api`
- Клієнтська частина: `http://localhost:5173`

## Стартові користувачі

Якщо таблиця `users` порожня, сервер створює користувачів:

| Логін | Роль |
| --- | --- |
| `admin` | `адмін` |
| `master1` | `майстер` |
| `master2` | `майстер` |
| `master3` | `майстер` |
| `manager1` | `менеджер` |

Пароль береться зі змінної `DEFAULT_USER_PASSWORD`. За замовчуванням: `123456`.

## Ролі та права доступу

| Роль | Доступ |
| --- | --- |
| `адмін` | Бачить усі замовлення, може керувати замовленнями, складом, клієнтами, заявками на деталі та видаляти записи. |
| `менеджер` | Бачить усі замовлення, може створювати замовлення, редагувати клієнтів, вести склад і обробляти заявки на деталі. |
| `майстер` | Бачить тільки ті замовлення, які призначені саме йому, вкладку клієнтів за своїми замовленнями, склад у режимі перегляду та налаштування інтерфейсу. Може змінювати статус і завершувати свої замовлення, але не може редагувати склад. |

Зв'язок майстра із замовленнями виконується через таблицю `masters`: поле `masters.username` має збігатися з логіном користувача в таблиці `users`. Наприклад, користувач `master1` бачить тільки замовлення, де призначений майстер з `username = 'master1'`.

## API

Основні маршрути:

- `POST /api/auth/login` - вхід у систему;
- `GET /api/auth/me` - перевірка поточного користувача;
- `GET /api/orders` - список замовлень;
- `POST /api/orders` - створення замовлення;
- `PUT /api/orders/:id/status` - зміна статусу;
- `GET /api/orders/:id/history` - історія статусів замовлення;
- `PUT /api/orders/:id/master` - призначення майстра для замовлення;
- `PUT /api/orders/:id/complete` - завершення ремонту з ціною та деталями;
- `DELETE /api/orders/:id` - видалення замовлення;
- `GET /api/masters` - список активних майстрів;
- `GET /api/parts` - список деталей;
- `POST /api/parts` - додавання деталі;
- `PUT /api/parts/:id` - оновлення кількості;
- `DELETE /api/parts/:id` - видалення деталі;
- `GET /api/part-requests` - список заявок на замовлення деталей;
- `POST /api/part-requests` - створення заявки майстром;
- `PATCH /api/part-requests/:id/status` - обробка заявки адміном або менеджером;
- `PUT /api/clients/:id` - оновлення телефону або email клієнта.

## SMS та Email

SMS надсилається тільки у двох випадках:

- при створенні нового замовлення;
- при завершенні ремонту.

Email надсилається при зміні статусу та при завершенні ремонту. Лист при завершенні містить суму до оплати.

## Сценарій демонстрації

1. Увійти як `admin`.
2. Створити нове замовлення з телефоном клієнта.
3. Призначити майстра вручну або показати автоматично призначеного майстра.
4. Показати, що замовлення отримало статус `прийнято`.
5. Перевести замовлення в `діагностика` або `ремонт`.
6. Відкрити завершення ремонту.
7. Додати вартість роботи та використані деталі.
8. Завершити ремонт і показати суму до оплати, списання деталей та Email/SMS-сповіщення.
9. Натиснути `Видати клієнту` і показати статус `видано`.
10. Відкрити історію статусів у деталях замовлення.
11. Перейти у звіти та показати фільтр дат, дохід і завантаження майстрів.

## Перевірка перед здачею

```bash
cd client
npm run lint
npm run build
```

Для сервера можна перевірити синтаксис:

```bash
cd server
node --check index.js
```

## Що показати в пояснювальній записці

- Постановку проблеми: контроль етапів ремонту, облік деталей, звітність.
- ER-діаграму таблиць бази даних.
- Скріншоти сторінок: вхід, список замовлень, створення замовлення, деталі, завершення ремонту, склад деталей, звіти.
- Приклад Email-листа з сумою до оплати.
- Приклад SMS-сповіщення.
- Опис структури проекту з цього README.
